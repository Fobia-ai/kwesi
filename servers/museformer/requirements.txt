# Museformer real inference -- UNVERIFIED, see README.md "Status" section.
#
# Python 3.8 (matches the vendored repo's own documented pin, same era as
# MuseCoco's). fairseq==0.10.2 is the same package version MuseCoco needs
# (both are muzic-era repos against the same fairseq fork API surface), so
# in principle this venv could share MuseCoco's venv -- kept separate here
# per kwesi.docs/02-architecture.md's one-venv-per-model rule, and because
# Museformer's own decoder pulls in extra kernel/dictionary machinery
# MuseCoco doesn't need.
fairseq==0.10.2
miditoolkit==0.1.16
# miditoolkit's own pianoroll submodule imports matplotlib.pylab
# unconditionally at import time (see miditoolkit/pianoroll/vis.py) -- not
# an optional/lazy import, so real MIDI decoding fails without this even
# though nothing here plots anything.
matplotlib
scipy
numpy==1.23.4
mido
fastapi
uvicorn[standard]
pydantic

# `uv venv` doesn't ship setuptools the way a system/virtualenv Python
# often implicitly does -- and the triton wheel torch pulls in for real
# inference (see README.md "Status") needs setuptools just to *import*
# (triton/runtime/build.py imports it at module load, not only when
# actually compiling a kernel). Without this, `import triton` -- and so
# real generation -- fails on a fresh `uv pip install -r` before ever
# reaching model code.
setuptools

# NOT pinned/installed here: the real MidiProcessor tokenizer/decoder
# Museformer's own README points at (github.com/btyu/MidiProcessor) isn't
# vendored a second time -- server.py reaches into servers/musecoco's
# already-vendored copy instead (see README.md), so musecoco must be
# installed too.
